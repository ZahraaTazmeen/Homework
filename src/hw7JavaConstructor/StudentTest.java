package hw7JavaConstructor;

public class StudentTest {
	public static void main(String[] args) {
		Student student = new Student();

		Student zahraa = new Student("Zahraa Tazmeen");

		// A default constructor doesn't have an argument, only appears once in a class,
		// and is initialized by an object.
		// A parameterized constructor is a constructor you can assign a value to
		// through an object, that appears once or multiple times in a class, and has to
		// come after a default constructor.

	}
}